{
  "name": "bfs",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:waves": "esbuild gradient-waves-init.js --bundle --format=iife --outfile=gradient-waves.bundle.js --platform=browser",
    "build:darkveil": "esbuild dark-veil-init.js --bundle --format=iife --outfile=dark-veil.bundle.js --platform=browser",
    "build:floatinglines": "esbuild floating-lines-init.js --bundle --format=iife --outfile=floating-lines.bundle.js --platform=browser",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "dependencies": {
    "ogl": "^1.0.11",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
